to convert no. to A,B,c
convert no to ascii value of alphabets for upper case starts from 65
take any n
SOP((char)(n+64));
use /t in the place of space for high no. of rows pattern so that they can adjust the pattern accordingly(also used for keeping format same in pattern having both single digit no and double digit no)
ex- instead of SOP("  ") use SOP(/t)
ways to right for  number
    n-i+1(for rows that gives 1,2,4,5,)
    j
    j%2
    patternSize(or star for me)
    for odd rows printing 2*i+1(increment)
    num=n*(n+1)/2
    num=num+2*i+1;
    num=num-(2*i+1);
For Q95 case:
col_mid=patternSize/2+1;

